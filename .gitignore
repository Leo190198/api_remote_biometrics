# Artefatos de compilacao e diagnostico
/AgenteBiometria.exe
# Builds de diagnostico (AgenteBiometria-diag7.exe, -forma.exe, ...).
/AgenteBiometria-*.exe
/testar-*.cmd
# Nome padrao do "go build" sem -o: vem do modulo (agente-biometria).
/agente-biometria.exe
/build-err.txt
*.log

# Saida do empacotamento MSI (WiX via NuGet).
instalador/msi/bin/
instalador/msi/obj/
*.msi
*.wixpdb

# Dados locais gerados pelo agente
# Templates biometricos NUNCA entram no repositorio: sao dado pessoal
# irrevogavel, e este repositorio e publico. Senha trocada vaza uma vez; digital
# vazada acompanha a pessoa pelo resto da vida.
template*.txt
# Arquivos de ambiente com template dentro (BIOMETRIA_*=...), do jeito que sai
# do banco para um teste local.
.env
.env.*
*.hash
cert.pem
key.pem
agente-*.json
origens-autorizadas.json

# Python, editores e sistema operacional
__pycache__/
*.py[cod]
.idea/
.vscode/
.DS_Store
Thumbs.db
